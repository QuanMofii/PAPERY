/** @type {import('next').NextConfig} */
const nextConfig = {
  
  // i18n: {
  //   locales: ['en', 'vi', 'jp'], 
  //   defaultLocale: 'en', 

  // },
  output: "standalone",
};
export default nextConfig;
